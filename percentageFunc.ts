function percentFunction(totalNumber: number, obtainedNumber: number) {
    return  obtainedNumber / totalNumber * 100
    
}
let percentAge = percentFunction(500, 350);
console.log(`${percentAge}%`);

